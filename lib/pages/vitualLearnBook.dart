import 'package:flutter/material.dart';

class VirtualLearnBook extends StatelessWidget {
  const VirtualLearnBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kategorien'),
    );
  }
}