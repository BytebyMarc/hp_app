import 'package:flutter/material.dart';

class VirtualLearnBook extends StatelessWidget {
  const VirtualLearnBook({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kategorien'),
    );
  }
}